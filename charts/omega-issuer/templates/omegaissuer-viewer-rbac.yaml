apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "omega-issuer.fullname" . }}-omegaissuer-viewer-role
  labels:
  {{- include "omega-issuer.labels" . | nindent 4 }}
rules:
- apiGroups:
  - omega-issuer.omegahome.net
  resources:
  - omegaissuers
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - omega-issuer.omegahome.net
  resources:
  - omegaissuers/status
  verbs:
  - get
